/**
  Inserts a block of css if IE is loaded.
  @version, int - the version of IE
  @strict, boolean - whether to inclue the css for only the version specified or also older versions
  
  Example:
    +ie(8)
      width 100%
**/
ie(version, strict = false)
  if ($is-IE == true)
    if(strict == true)
      if ($IE-version == version)
        {block}
    else
      if ($IE-version <= version)
        {block}